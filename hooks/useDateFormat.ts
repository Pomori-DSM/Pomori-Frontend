export const useDateFormat = (date: Date) => {
  const formatDate = { year: "", month: "", day: "" };
  const newDate = new Date();
  // const [newDay, newMonth, new] = date.toString().split(" ");
  const days = {
    Mon: "월",
    Tue: "화",
    Wed: "수",
    Thu: "목",
    Fri: "금",
    Sat: "토",
    Sun: "일",
  };
  return formatDate;
};
